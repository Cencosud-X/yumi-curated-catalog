/// <reference types="react" />
interface IProps {
    showModal: boolean;
    setShowModal: (data: boolean) => void;
}
export declare const ModalEdit: ({ showModal, setShowModal }: IProps) => JSX.Element;
export {};
